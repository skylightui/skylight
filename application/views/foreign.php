<?php
    // This is empty view used for loading foreign content
    // (held outside of the normal CodeIgniter views directory)
    //
    // This must be performed from within a view so that the
    // buffering is applied in order
    include($load);
?>